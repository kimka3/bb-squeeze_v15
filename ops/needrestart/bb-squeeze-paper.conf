# Temporary policy for the existing fixed paper campaign only.
# Security packages still install; only this service's needrestart restart
# is deferred. Review urgent library fixes during the campaign and perform
# controlled maintenance when needed. This does not suppress restart notices.
# Expires at 2026-10-10T23:21:39Z; do not extend it to prolong the campaign.
if (time() < 1791674499) {
    $nrconf{override_rc}{qr(^bb-squeeze-paper\.service$)} = 0;
}
1;
