"""Live execution for BB Squeeze A on Lighter.

The decision logic is NOT here — it is in src/live_core.py, shared byte-for-byte
with the backtest. This package only feeds that core real data and turns its
output into exchange orders.
"""
